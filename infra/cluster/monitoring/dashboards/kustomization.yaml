apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: grafana-dashboard-server-usage
    files:
      - server-usage.json

generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"
