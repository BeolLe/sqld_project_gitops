apiVersion: v1
kind: Service
metadata:
  name: sqld-frontend
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: sqld-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP

